# suh-ai-server (Flask) 프로젝트 메타정보
# version은 version.yml과 자동 동기화됩니다 (.github/scripts/version_manager.py — python 타입)
# 의존성은 기존 requirements.txt로 관리합니다 (이 파일은 버전 관리·메타정보 용도)
[project]
name = "suh-ai-server"
version = "2.0.54"
requires-python = ">=3.10"
